# 🚀 1. 全体アーキテクチャ図（設計書レベル）

```
┌──────────────┐                ┌────────────┐
│  AWS CONTROL │                │ AWS STORAGE│
│  PLANE       │                │ + COMPUTE  │
│              │                │            │
│  API GW      │◄ HTTP(S) ◄────► Lambda     │
│              │                │            │
│   SQS QUEUE  │◄──────┐        │   S3       │
│              │       │        │            │
│   DLQ        │       │        │            │
└──────────────┘       │        └────────────┘
                       │
                       ▼ HTTPS (Proxy)
                ┌─────────────────┐
                │ ON-PREMISE HOST │
                │ (常駐バッチ)    │
                │ - Python Agent  │
                │ - ODBC → Oracle │
                │ - AWS SDK/Boto3 │
                └─────────────────┘
                        │
                        ▼ ODBC (Oracle 18c)
                ┌─────────────────┐
                │ Oracle DB (閉域)│
                └─────────────────┘
```

### 各要素の役割

- **SQS Queue**
  AWS 側で SQL 実行リクエストをキューイング。クライアントが PUT、オンプレ側が PULL で取得します。
  _セキュリティベストプラクティスとしてパブリックアクセスを禁止、最小権限付与、サーバー側暗号化を有効化推奨_ ([AWS ドキュメント][1])

- **Dead Letter Queue (DLQ)**
  リトライ失敗メッセージを隔離して障害監視と復旧を容易にする機構。

- **オンプレ常駐 Python バッチ**
  SQS Polling、SQL実行（ODBC）、結果返却（S3 または結果用 API）を担当。

- **Lambda + API Gateway**
  結果受け取り、管理画面更新、ステータス API など。

- **S3**
  SELECT 結果（~7–8MB程度）をファイルとして保存。URL やキーを SQS メッセージとして AWS 側に通知。

---

# 🛂 2. IAM ポリシー（最小権限版）

以下は **オンプレ Python エージェント用 IAM ポリシー** の完全版サンプルです。

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "SQSReceiveOnly",
      "Effect": "Allow",
      "Action": [
        "sqs:ReceiveMessage",
        "sqs:ChangeMessageVisibility",
        "sqs:DeleteMessage",
        "sqs:GetQueueUrl"
      ],
      "Resource": "arn:aws:sqs:ap-northeast-1:<AWS_ACCOUNT_ID>:<REQUEST_QUEUE_NAME>"
    },
    {
      "Sid": "SQSSendResponse",
      "Effect": "Allow",
      "Action": ["sqs:SendMessage"],
      "Resource": "arn:aws:sqs:ap-northeast-1:<AWS_ACCOUNT_ID>:<RESPONSE_QUEUE_NAME>"
    },
    {
      "Sid": "S3PutObject",
      "Effect": "Allow",
      "Action": ["s3:PutObject", "s3:PutObjectAcl"],
      "Resource": "arn:aws:s3:::<RESULT_BUCKET_NAME>/*"
    }
  ]
}
```

### 補足

- `sqs:ChangeMessageVisibility` は受信後に Visibility Timeout を制御する必要があるため含める必要あり。([Reddit][2])
- S3 は SELECT 結果をアップロードするため PutObject アクションのみ許可。

---

# 🐍 3. Python 実装サンプル（SELECT + INSERT + UPDATE）

以下は **オンプレ Python バッチの主要処理例（完全版）** です。

```python
import os
import json
import pyodbc
import boto3
from botocore.config import Config

# ---- AWS 設定 ----
QUEUE_URL = os.getenv("AWS_REQ_QUEUE_URL")
RESP_QUEUE_URL = os.getenv("AWS_RESP_QUEUE_URL")
RESULT_BUCKET = os.getenv("RESULT_BUCKET")
REGION = "ap-northeast-1"

boto_config = Config(
    retries = {"max_attempts": 3},
    connect_timeout=5
)

sqs = boto3.client("sqs", region_name=REGION, config=boto_config)
s3 = boto3.client("s3", region_name=REGION, config=boto_config)

# ---- Oracle 接続 ----
conn = pyodbc.connect("DSN=YourOracleDSN;UID=USERNAME;PWD=PASSWORD")
conn.autocommit = False


def process_message(msg):
    body = json.loads(msg['Body'])
    sql = body.get("sql")
    request_id = body.get("id")

    cur = conn.cursor()
    try:
        cur.execute(sql)

        # SELECT
        if sql.strip().lower().startswith("select"):
            rows = cur.fetchall()
            cols = [column[0] for column in cur.description]
            result = [dict(zip(cols, row)) for row in rows]

            # 結果 JSON を S3 へアップ
            key = f"{request_id}.json"
            s3.put_object(
                Bucket=RESULT_BUCKET,
                Key=key,
                Body=json.dumps(result),
                ContentType="application/json"
            )

            payload = {"id": request_id, "result_s3_key": key}

        else:
            # INSERT/UPDATE
            conn.commit()
            payload = {"id": request_id, "affected_rows": cur.rowcount}

    except Exception as e:
        conn.rollback()
        payload = {"id": request_id, "error": str(e)}

    send_response(payload)


def send_response(payload):
    sqs.send_message(
        QueueUrl=RESP_QUEUE_URL,
        MessageBody=json.dumps(payload)
    )


def main_loop():
    while True:
        resp = sqs.receive_message(
            QueueUrl=QUEUE_URL,
            MaxNumberOfMessages=1,
            WaitTimeSeconds=10
        )
        messages = resp.get("Messages")
        if not messages:
            continue

        for msg in messages:
            process_message(msg)
            sqs.delete_message(
                QueueUrl=QUEUE_URL,
                ReceiptHandle=msg["ReceiptHandle"]
            )

if __name__ == "__main__":
    main_loop()
```

---

# ⚠ 4. 障害設計（設計書レベル）

## 🧯 ① 障害カテゴリ

| 障害種別                | 発生原因               | 対策                      |
| ----------------------- | ---------------------- | ------------------------- |
| SQS 取得失敗            | ネットワーク/認証      | 再試行（バックオフ）、DLQ |
| SQL 実行エラー          | SQL 文法/権限          | ログ出力 + 結果エラー返却 |
| S3 アップロード失敗     | 接続/権限              | リトライ・SNS 通知        |
| Oracle コネクション切断 | 長時間 Idle            | 再接続処理                |
| オンプレ Python 停止    | エージェントクラッシュ | SystemD/監視              |

---

## 🧰 ② 冗長性と回復設計

### SQS

- **Dead Letter Queue (DLQ)**
  リトライ最大 3 回超過時に隔離して調査容易化。

### 健全性監視サービス

- **CloudWatch Metrics**
  - SQS ApproximateAgeOfOldestMessage（古い未処理キュー）
  - SQS NumberOfMessagesSent / Received / Deleted
  - Lambda Errors（結果受信パス）

- **アラーム**
  - SQS DLQ 増加検知
  - S3 PutObject 失敗率増加

---

## 🖥 オンプレの監視設計

- **systemd / Supervisor** で常駐プロセス化

- **ログ出力先** を分けて
  - `oracle_exec.log`
  - `aws_sqs.log`
  - `error.log`

- 異常時は **メール / Slack 通知**

---

# 🔍 5. セキュリティと運用ベストプラクティス

### 🔒 SQS セキュリティ

- キューポリシーで **公開アクセス禁止**
- **HTTPS 強制**（SecureTransport 条件） ([AWS ドキュメント][1])
- サーバー側暗号化（SQS SSE）推奨 ([AWS ドキュメント][1])

---

# 📌 6. 追加検討ポイント（運用時）

## レート制御

オンプレバッチは短時間に SQL を連投しないよう

- スロットリング
- VisibilityTimeout 適切調整

## 監査ログ

Oracle 側で SQL 実行ログを残す（誰が何を実行したか追跡）

## バージョン管理

- Python スクリプトは Git 管理
- AWS 用リソースは IaC（CloudFormation/Terraform）

## 要件詳細・補足

- **APP_ID の検証**
  - SQS メッセージに `app_id` 項目を追加し、エージェントはホワイトリストに登録された値のみ処理する。
- **SQL 制約**
  - SELECT 文は `WHERE` に `MK_DATE` 範囲指定を必須とし、簡易パーサーでチェックする。
  - INSERT/UPDATE は対象が 1 レコードのみであることを確認する。
- **データ返却方式の評価**
  - 最大 7–8MB 程度の結果であればコストが最も安価で、後続処理にとって扱いやすいのは S3 出力＋URL 返却。
  - SQS のサイズ制限や追加構成コストを考えると他方式は劣る。
- **スケール想定**
  - 同時リクエスト 3 件程度、1 日 20 件前後と軽負荷。特別な拡張は不要。
- **障害復旧**
  - 現時点では詳細な復旧手順は未定。一般的なログ確認と再起動運用を前提にし、後日正式化する。
  - オンプレ Python 停止時は SNS などを通じて通知可能。オンプレバッチからは `boto3.client('sns').publish()` を呼び出し、例外ハンドラや終了時にメッセージを送るだけで十分。処理急迫度は低く、復旧時間には余裕あり。
- **データサイズ制限**
  - 7–8MB より大きな結果は問い合わせ対象外とし、エージェントはそれを検出してエラーとする。
- **AWS 側ログ保持**
  - エージェントの実行ログは CloudWatch Logs に出力し、適宜保持期間を設定する。
